services:
  prod-quiz:
    image: emils-nuc-server:5000/quiz:3d8004b91
    container_name: prod-quiz
    restart: unless-stopped
    env_file:
      - stacks.env
      - stacks.prod.env
    networks:
      - core-network
networks:
  core-network:
    external: true
