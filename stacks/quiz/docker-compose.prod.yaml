services:
  prod-quiz:
    image: emils-nuc-server:5000/quiz:9cfcdda
    container_name: prod-quiz
    restart: unless-stopped
    env_file:
      - stacks.env
      - stacks.prod.env
    networks:
      - core-network
networks:
  core-network:
    external: true
